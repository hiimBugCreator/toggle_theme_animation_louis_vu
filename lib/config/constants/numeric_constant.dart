import 'dart:ui';

class NumericConstant {
  static const double buttonSize = 60.0;
  static const double paddingButton = 12;
  static const Size toggleSize = Size(buttonSize * 5, buttonSize * 1.85);
  static const Duration animationDuration = Duration(milliseconds: 500);
  static const Duration hoverAnimationDuration = Duration(milliseconds: 200);
  static const int numberOfCirclesShine = 3;
}
